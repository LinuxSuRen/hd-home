filename: "{{.Name}}-{{.Version}}-{{.OS}}"
binary: marp
targetBinary: marp
tar: true          # if this is a tar file
formatOverrides:   # set the file extension for different platforms
  windows: zip
  linux: tar.gz
categories:
- markdown
- md
