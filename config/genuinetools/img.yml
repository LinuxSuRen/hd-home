filename: "{{.Name}}-{{.OS}}-{{.Arch}}"
binary: img         # please provide a binary file name if the release filename is not the final one
targetBinary: img   # a desired target binary file name
tar: false          # if this is a tar file
categories:
- container
