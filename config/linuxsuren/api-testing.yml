filename: "atest-{{.OS}}-{{.Arch}}"
binary: atest         # please provide a binary file name if the release filename is not the final one
tar: true          # if this is a tar file
formatOverrides:   # set the file extension for different platforms
  windows: zip
  linux: tar.gz
categories:
- test
